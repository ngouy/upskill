# Release Notes

## v1.0.0 — TBD

### Added
- `manager` — unified inventory for all installed skills (plugin + local), with periodic update nudges
- `publisher` — full scaffold → git → GitHub → versioning → release pipeline
- `doctor` — skill quality and security analyst (Curator and Guardian modes)
- `auditor` — point-in-time session snapshot through the lens of loaded skills
- Optional upskill watermark in SKILL.md frontmatter and README footer (opt-out via state file)

### Notes
Initial release.
